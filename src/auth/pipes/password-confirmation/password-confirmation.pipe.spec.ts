import { PasswordConfirmationPipe } from './password-confirmation.pipe';

describe('PasswordConfirmationPipe', () => {
  it('should be defined', () => {
    expect(new PasswordConfirmationPipe()).toBeDefined();
  });
});
